// Configuração da API Backend
const API_CONFIG = {
    // Para desenvolvimento local, use: http://localhost:3000
    // Para produção, use a URL do Render
    BASE_URL: 'https://seu-app.onrender.com' // Substitua pela URL do seu app no Render
};
